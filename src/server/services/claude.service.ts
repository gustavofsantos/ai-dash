export class ClaudeService {
  // Placeholder for Claude-specific logic (e.g. transcript parsing, specialized event handling)
  async parseTranscript(path: string) {
    // To be implemented when we have samples of Claude's local transcripts
    return null;
  }
}
